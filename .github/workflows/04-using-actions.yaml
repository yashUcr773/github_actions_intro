name: 04-using-actions
on: 
    push:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
            - name: cd repo
              run: cd react-app
            - name: print_folders
              run: ls -a